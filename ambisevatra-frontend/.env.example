# ──────────────────────────────────────────────────
# AmbiSevatra Frontend Configuration
# Copy this to .env.local and fill in your values
# ──────────────────────────────────────────────────

# Backend API (just the base URL — /api/v1 is appended in code)
VITE_API_URL=http://localhost:8000

# Google Sign-In (get your Client ID from Google Cloud Console)
VITE_GOOGLE_CLIENT_ID=your-google-client-id-here
